<Window x:Class="FocusPanel.Views.PomodoroFloatingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FocusPanel.Views"
        mc:Ignorable="d"
        Title="Focus Timer" Height="80" Width="240"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    
    <Window.Resources>
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#DDFFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse x:Name="bg" Fill="{TemplateBinding Background}" Opacity="0"/>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Fill" Value="White"/>
                                <Setter TargetName="bg" Property="Opacity" Value="0.15"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bg" Property="Opacity" Value="0.3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Main Container with Shadow -->
        <Border CornerRadius="16" BorderBrush="#33FFFFFF" BorderThickness="1" Margin="10">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#FF2D2D2D" Offset="0"/>
                    <GradientStop Color="#FF1E1E1E" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.6" Direction="270" Color="Black"/>
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="15,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Time Display -->
                    <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="5,0,0,0">
                        <TextBlock Text="{Binding TimerDisplay}" 
                                   Foreground="White" 
                                   FontSize="26" 
                                   FontWeight="Bold"
                                   FontFamily="Segoe UI"
                                   VerticalAlignment="Center">
                             <TextBlock.Effect>
                                <DropShadowEffect BlurRadius="2" ShadowDepth="1" Opacity="0.5"/>
                             </TextBlock.Effect>
                        </TextBlock>
                        <TextBlock Text="{Binding StatusMessage}"
                                   Foreground="#AAAAAA"
                                   FontSize="10"
                                   Margin="2,-2,0,0"
                                   TextTrimming="CharacterEllipsis"/>
                    </StackPanel>

                    <!-- Controls -->
                    <StackPanel Orientation="Horizontal" Grid.Column="1" VerticalAlignment="Center">
                        <Button Command="{Binding PauseCommand}" ToolTip="Pause" Style="{StaticResource IconButtonStyle}" Content="⏸" FontSize="14"/>
                        <Button Command="{Binding StartCommand}" ToolTip="Start" Style="{StaticResource IconButtonStyle}" Content="▶" FontSize="14"/>
                        <Button Command="{Binding ResetCommand}" ToolTip="Stop" Style="{StaticResource IconButtonStyle}" Content="⏹" FontSize="14" Foreground="#FF6B6B"/>
                    </StackPanel>
                </Grid>

                <!-- Progress Bar at Bottom -->
                <ProgressBar Grid.Row="1" 
                             Value="{Binding Progress}" Maximum="100" Minimum="0"
                             Height="3" 
                             Background="Transparent" 
                             BorderThickness="0"
                             VerticalAlignment="Bottom">
                    <ProgressBar.Foreground>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#FF00C853" Offset="0"/>
                            <GradientStop Color="#FF69F0AE" Offset="1"/>
                        </LinearGradientBrush>
                    </ProgressBar.Foreground>
                </ProgressBar>
            </Grid>
        </Border>
    </Grid>
</Window>