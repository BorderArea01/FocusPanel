<Window x:Class="FocusPanel.Views.ScreenBorderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Focus Border"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        WindowState="Maximized"
        IsHitTestVisible="False">
    
    <Border BorderThickness="5">
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FFFF0000" Offset="0.0"/>
                <GradientStop Color="#FFFFFF00" Offset="0.2"/>
                <GradientStop Color="#FF00FF00" Offset="0.4"/>
                <GradientStop Color="#FF00FFFF" Offset="0.6"/>
                <GradientStop Color="#FF0000FF" Offset="0.8"/>
                <GradientStop Color="#FFFF00FF" Offset="1.0"/>
                <LinearGradientBrush.Transform>
                    <RotateTransform Angle="0" CenterX="0.5" CenterY="0.5"/>
                </LinearGradientBrush.Transform>
            </LinearGradientBrush>
        </Border.BorderBrush>
        
        <Border.Triggers>
            <EventTrigger RoutedEvent="Border.Loaded">
                <BeginStoryboard>
                    <Storyboard RepeatBehavior="Forever">
                        <DoubleAnimation Storyboard.TargetProperty="(Border.BorderBrush).(LinearGradientBrush.Transform).(RotateTransform.Angle)"
                                         From="0" To="360" Duration="0:0:5"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>
    </Border>
</Window>